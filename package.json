{
  "name": "will-it-rain",
  "version": "1.0.0",
  "description": "AWS Lambda that checks tomorrow's rain forecast and sends Discord notifications",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node esbuild.config.mjs",
    "package": "npm run build && cd dist && zip -r ../will-it-rain.zip index.mjs",
    "dev": "tsx --env-file=.env src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "esbuild": "^0.20.0",
    "vitest": "^1.6.0",
    "tsx": "^4.10.0",
    "@types/aws-lambda": "^8.10.136",
    "@types/node": "^20.12.0"
  }
}
